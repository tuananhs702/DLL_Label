name: Run Jupyter Notebook Manually

on:
  workflow_dispatch:  # Chỉ chạy khi kích hoạt thủ công từ GitHub Actions

jobs:
  run-notebook:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install --upgrade pip
          pip install numpy pandas scikit-learn xgboost underthesea papermill

      - name: Verify Papermill Installation
        run: |
          python -m pip show papermill
          python -m papermill --help

      - name: Run Jupyter Notebook
        run: |
          python -m papermill DLM_label.ipynb output.ipynb

      - name: Commit and Push Output
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add output.ipynb
          git commit -m "Auto-update output notebook" || echo "No changes to commit"
          git push
